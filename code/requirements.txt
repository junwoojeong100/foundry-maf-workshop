# Microsoft Foundry & Agent Framework 워크샵 — 로컬 개발용 의존성
#
# 설치: pip install -r requirements.txt
#
# (각 hosted agent 폴더(maf_basic/ 등)에는 code deployment용 requirements.txt가 따로 있습니다.)

# 2026-07-11에 Python 3.13으로 검증한 버전입니다.
# 워크샵 중 API 변경으로 예제가 깨지지 않도록 정확한 버전을 사용합니다.

# --- Foundry SDK (Lab 02: 기초 예제 1개) ---
azure-ai-projects==2.3.0

# --- Microsoft Agent Framework 코어 (Lab 03~06) ---
# agent-framework 메타 패키지는 불필요한 선택 기능까지 설치하므로 사용하지 않습니다.
agent-framework-core==1.11.0

# --- Foundry 전용 클라이언트: FoundryChatClient / FoundryAgent ---
agent-framework-foundry==1.10.1

# --- hosted agent를 로컬에서 serve 하기 위한 호스팅 런타임 (Lab 06) ---
agent-framework-foundry-hosting==1.0.0a260709

# --- 인증 (공통) ---
azure-identity==1.25.3

# --- 편의: .env 파일 로딩 ---
python-dotenv==1.2.2

# --- Lab 04 도구 예제의 파라미터 스키마 ---
pydantic==2.13.4
